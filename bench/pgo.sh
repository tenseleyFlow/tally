#!/bin/sh
# Profile-guided build (opt-in, clang/llvm). Arrives with sprint 06 — packaged
# builds stay plain -O3 -flto for reproducibility either way.
echo "pgo: not wired yet (sprint 06); use 'gmake release'"
exit 0
